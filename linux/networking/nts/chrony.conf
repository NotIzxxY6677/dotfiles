# Purpose: Privacy-hardened, authenticated time synchronization (NTS-only, non-smearing).
# Scope: Global chronyd configuration.
# Dependencies: Working DNS resolution (any resolver) for NTS-KE hostnames;
#               a roughly correct clock at boot (see driftfile below and the
#               -s flag in chronyd's startup options).
# Standard: RFC 8915 (NTS), RFC 5905 (NTPv4), RFC 8633 (NTP BCP).
#
# Time sources are strictly NTS-only and non-smearing, selected for operator,
# network-path, and stratum diversity. NOTE: As of 2026 no Asian national
# metrology institute or government time service offers NTS; they are
# NTP-only. Cloudflare anycast is the only production NTS source with points
# of presence in Asia; the remaining anchors are European institutions
# reached over unicast.

# Cloudflare ; Anycast (global) ; stratum 3
# Widest anycast footprint of any NTS source -> lowest-latency anchor.
server time.cloudflare.com iburst nts

# TimeNL / SIDN Labs ; Unicast (Netherlands) ; stratum 1
# Institutional (SIDN, the .nl registry). NOTE: these two hosts are also
# enrolled in the NTP Pool; they are named, operator-run servers, not
# anonymous pool members, so they satisfy the institutional-NTS requirement.
server ntppool1.time.nl iburst nts
server ntppool2.time.nl iburst nts

# PTB (German national metrology institute) ; Unicast (Germany) ; stratum 1
server ptbtime1.ptb.de iburst nts

# Netnod ; Anycast, but all nodes are sited in Sweden ; stratum 1
server nts.netnod.se iburst nts

# Unauthenticated sources must never be selected. Any packaged
# `sourcedir /run/chrony-dhcp` default is deliberately omitted here, so
# DHCP-supplied servers are never loaded, let alone selected.
authselectmode require

# Require a minimum of 2 agreeing sources before disciplining the clock.
minsources 2

# Skip certificate validity-time (activation/expiration) checks for NTS-KE
# until the clock has been corrected once. Protects the first key exchange
# from failing due to an incorrect RTC; applies only to chronyd's own TLS.
nocerttimecheck 1

# Authoritative leap-second table (IERS/NIST schedule, shipped by tzdata).
# Directive added in chrony 4.6, so this file requires chrony >= 4.6. With
# the default `leapsecmode system`, a leap second is applied as a true kernel
# step -- never slewed or smeared.
leapseclist /usr/share/zoneinfo/leap-seconds.list

# Persist NTS cookies/keys across restarts (skips NTS-KE round trips; note
# that cookies cannot substitute for DNS resolution of source hostnames).
ntsdumpdir /var/lib/chrony

# Step the clock if the offset exceeds 1 second during the first 3 updates.
makestep 1.0 3

# Frequency-error (drift) history. Its modification time doubles as the
# cold-boot clock floor applied by `chronyd -s`.
driftfile /var/lib/chrony/drift

# Periodically copy synchronized system time back to the RTC (kernel
# 11-minute mode).
rtcsync

# Directory for daemon logs.
logdir /var/log/chrony
